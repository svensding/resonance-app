{
  "name": "resonance-echo-deck-netlify",
  "version": "1.0.0",
  "description": "Resonance Echo Deck application for Netlify",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html metadata.json dist/ && cp -R components services hooks dist/ && esbuild index.tsx --bundle --outfile=dist/bundle.js --define:process.env.API_KEY=\\\"$GEMINI_API_KEY\\\" --loader:.ts=tsx --loader:.js=jsx --jsx=automatic --platform=browser --format=esm && cp -R public/* dist/ 2>/dev/null || true",
    "lint": "echo 'Linting not yet configured'"
  },
  "dependencies": {
    "@google/genai": "^1.3.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.5.1",
    "esbuild": "^0.25.5",
    "vite": "^6.3.5"
  },
  "engines": {
    "node": ">=18"
  },
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/svensding/resonance-app.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/svensding/resonance-app/issues"
  },
  "homepage": "https://github.com/svensding/resonance-app#readme"
}
